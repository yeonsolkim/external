---
layout: post
title: "3. Linear Combinations and Systems of Linear Equations"
date: 2026-06-22 00:00:00 +0900
category_path:
  - 1. Mathematics
  - 3. Linear Algebra
  - 1. Vector Spaces
created_at: 2026-08-06 13:53:49 +0900
last_modified_at: 2026-08-17 18:09:35 +0900
---
**Definition 1.3.1.** Let $A$ be a set equipped with an associative addition operation and an additive identity $0_A$, let $n \in \mathbb{N}$, and let 

$$a:[n]\longrightarrow A$$ 

be a function. The *finite sum* of the family $\lbrace a_i\rbrace_{i\in [n]}$ denoted by

$$  
\sum_{i=1}^{n} a_i,  
$$

is defined recursively as follows:

$$  
\sum_{i=1}^{0} a_i = 0_A,  
$$

and, for each $k \in \mathbb{N}$,

$$  
\sum_{i=1}^{k+1} a_i  
=  
\left(\sum_{i=1}^{k} a_i\right) + a_{k+1}.  
$$

<br>

**Definition 1.3.2.** Let $\mathbb V$ be a vector space over $\mathbb F$, and let $S \subseteq \mathbb V$. A vector $v \in \mathbb V$ is a *linear combination of the vectors in* $S$ if and only if

$$
\text{there exist}\quad n\in\mathbb N,\quad

u:[n] \to S,\quad

a:[n] \to\mathbb F 
\quad \text{such that} \quad v = \sum_{i=1}^{n} a_i u_i.
$$

&emsp; If $v$ is a linear combination of the vectors in $S$ with $n > 0,$ we say that $v$ is a *linear combination of* $u_1, \dots, u_n,$ and we call $a_1, \dots, a_n$ the *coefficients* of the linear combination.<br>
<br>


**Definition 1.3.3.** Let $S$ be a subset of a vector space $\mathbb V$ over a field $\mathbb F.$ The *span* of $S$ is the set $\text{span} (S)$ defined by

$$
\operatorname{span}(S)

=

\left\lbrace

v\in \mathbb V : v\text{ is a linear combination of the vectors in } S

\right\rbrace.
$$

Note that we have $\text{span} (\varnothing) = \lbrace \mathit 0\rbrace$ by the definition.

<br>

**Theorem 1.3.4.** Let $S$ be a subset of a vector space $\mathbb V$ over a field $\mathbb F.$ Then $\text{span} (S)$ is a subspace of $\mathbb V$ that contains $S.$ Moreover, any subspace of $\mathbb V$ that contains $S$ contains $\text{span} (S).$<br><br>
*Proof.* If $S=\varnothing$ then $\text{span} (\varnothing) = \lbrace \mathit 0\rbrace$ so that $\text{span} (\varnothing)$ is a subspace of $\mathbb V$ that contains $S$ and is contained any subspace of $\mathbb V$.<br>
&emsp; If $S\ne \varnothing$ then there is a vector $z$ in $S$. Since $0z=\mathit 0,$ 

$$\mathit 0\in \text{span} (S).$$ 

Let $x,y\in \text{span} (S).$ Then there exist $u_1,\dots,u_m,v_1,\dots,v_n\in S$ and $a_1,\dots,a_m,b_1,\dots,b_n\in \mathbb F$ such that 

$$ x = a_1u_1 + \cdots + a_mu_m \quad \text{and} \quad y = b_1v_1 + \cdots + b_nv_n.$$

Then 

$$ x+y = a_1u_1 + \cdots + a_mu_m + b_1v_1 + \cdots + b_nv_n$$

and, for any $c\in \mathbb F,$ 

$$ cx = (ca_1)u_1 + \cdots + (ca_m)u_m$$

are linear combinations of the vectors in $S$, so that $x+y$ and $cx$ are in $\text{span} (S)$. Thus $\text{span} (S)$ is a subspace of $\mathbb V$. Furthermore, if $v\in S$ then $v=1\cdot v \in \text{span}(S)$; so $S\subseteq \text{span} (S)$.<br>
&emsp; Now let $\mathbb W$ be a subspace of $\mathbb V$ that contains $S$. If $w\in \text{span} (S)$, then 

$$ w = c_1w_1 + \cdots + c_kw_k$$ 

for some $w_1,\dots,w_k\in S$ and $c_1,\dots,c_k\in \mathbb F.$ Since $S\subseteq \mathbb W$, we have $w_1,\dots,w_k\in \mathbb W.$ Therefore $w\in \mathbb W$ and thus $\text{span} (S)\subseteq \mathbb W$. <span class="qed">$\square$</span><br>
<br>

  
## References  
  
1. Friedberg, S. H., Insel, A. J., & Spence, L. E. (2025). *Linear algebra* (5th ed.). Pearson Education South Asia Pte Ltd.
{:reference}
